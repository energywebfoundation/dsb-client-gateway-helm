apiVersion: v1
kind: Service
metadata:
  name: {{ include "ddhub-client-gateway-ui.fullname" . }}
  labels:
    {{- include "ddhub-client-gateway-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ddhub-client-gateway-ui.selectorLabels" . | nindent 4 }}
